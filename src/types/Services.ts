export type ServicesType = {
  title: string;
  description?: string;
  content: string[];
  image?: string;
  link?: string;
};

export type Portfolio = {
  src: string;
  description: string;
  link?: string;
};
